name: e_pack_final
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Functional Programming
  dartz: ^0.10.0

  # Value Equity
  equatable: ^2.0.3

  # Text mask for numbers
  extended_masked_text: ^2.3.1

  flutter_pw_validator: ^1.2.1

  # Internet Connection Checker
  internet_connection_checker: ^0.0.1+3

  # Bloc for Business Logic Handling
  flutter_bloc: ^8.0.1

  # Service Locator
  get_it: ^7.2.0

  # HTTP Requests
  http: ^0.13.4
  dio: ^4.0.4

  # DateTime Picker
  syncfusion_flutter_datepicker: ^19.2.62
  flutter_generate_assets: ^0.4.1

  #Local Storage
  shared_preferences: ^2.0.13

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/texts/
  #   - images/a_dot_ham.jpeg